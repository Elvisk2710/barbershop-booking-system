{
  "name": "gentlemans-grooming-bar-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Gentleman's Grooming Bar - Full Stack Monorepo (Next.js + Node/Express + Prisma)",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n \"backend,frontend\" -c \"cyan,magenta\" \"npm run dev -w apps/backend\" \"npm run dev -w apps/frontend\"",
    "dev:frontend": "npm run dev -w apps/frontend",
    "dev:backend": "npm run dev -w apps/backend",
    "build": "npm run build --workspaces --if-present",
    "db:generate": "npm run db:generate -w apps/backend",
    "db:push": "npm run db:push -w apps/backend",
    "db:seed": "npm run db:seed -w apps/backend",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.4.5"
  }
}
